baseURL = 'https://kind-pond-0cf39fd1e.azurestaticapps.net'
languageCode = 'en-us'

# Change the default theme to be use when building the site with Hugo
theme = "dark-theme-editor"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]

